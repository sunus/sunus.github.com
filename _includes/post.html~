<article>
	<h1>{{ post.title }}</h1>
	<header>
		<time>{{ post.date | date: "%B %e, %Y" }}</time>
	</header>
	{{ post.content }}
	<footer>
	</footer>
</article>

{% for related in site.related_posts %}
	<a href="">dfdf{{ related.title }}</a>
{% endfor %}
