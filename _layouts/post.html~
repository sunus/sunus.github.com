---
layout: layout
---

<!-- Remove comments function temporarily -->

<article>
	<h1>{{ page.title }}</h1>
	<header>
		<time>{{ page.date | date: "%B %e, %Y" }}</time> ~ <a href="{{ page.url }}">Permalink</a>
	</header>
	{{ content }}
</article>

<h2>Related posts</h2>
<ul>
{% for related in site.related_posts limit:5 %}
	<li><a href="{{ related.url }}">{{ related.title }}</a></li>
{% endfor %}
</ul>

